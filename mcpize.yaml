# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Bharat Stock Market"
slug: "bharat-stock-market"

runtime: python

build:
  install: "pip install -r requirements.txt"

start:
  type: stdio
  command: "python"
  args:
    - "bharath_mcp.py"
    - "--mcp"
